{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${cwd}/scripts/build.ps1"
        },
        {
            "label": "Run Editor",
            "type": "shell",
            "command": "${cwd}/scripts/run_editor.ps1"
        }
    ]
}